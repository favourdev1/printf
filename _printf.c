#include <stdio.h>
#include <strarg.h>
/**
 * _printf - check if theres is a valid format specifier
 * specifier
 * @format: possible format specifier
 * Return: pointer to valid function or NULL
 */

int _printf(const char *format, ...);
char* convert(unsigned int, int);

int _printf(const char *format, ...)
{
	
}
